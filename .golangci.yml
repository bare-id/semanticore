version: "2"

# Uses golangci-lint's default linters (errcheck, govet, ineffassign,
# staticcheck, unused) plus misspell, and gofmt/goimports as formatters.
linters:
  enable:
    - misspell

formatters:
  enable:
    - gofmt
    - goimports
